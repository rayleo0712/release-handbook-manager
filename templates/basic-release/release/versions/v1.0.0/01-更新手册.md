# {版本号} 更新手册

## 1. 基本信息

- 版本号：
- 上一版本号：
- 当前状态：
- 版本负责人：
- 计划发布时间：
- 关联说明：

## 2. 涉及模块

- `backend`：
- `frontend`：
- `client`：
- `uniapp`：

## 3. 本版本变更摘要

### 3.1 新增功能

- 

### 3.2 接口权限表

| 接口编号 | 接口名称 | 请求方式 | 所属模块 | EPS 标识 | 权限说明 |
|---|---|---|---|---|---|
| API-001 | {接口名称示例} | POST/GET | {模块名} | 标识A、标识B、标识C | {权限描述} |
| API-002 | {接口名称示例} | POST/GET | {模块名} | 标识D、标识E | {权限描述} |
| API-003 | {接口名称示例} | POST/GET | {模块名} | 标识F | {权限描述} |

### 3.3 功能调整

- 

### 3.4 缺陷修复

- 

### 3.5 兼容性处理

- 

### 3.6 风险说明

- 

## 4. 必须脚本化的变更

### 4.1 数据库结构变更

- 是否涉及：是 / 否
- 变更说明：
- 对应脚本：
  - `02-db-001-`
- 执行顺序：
- 执行后校验：

#### 4.1.2 Bash四场景模板

```bash
# ============================================================
#  变量准备区（请在执行前替换以下变量值）
# ============================================================
export DB_HOST="数据库主机地址"
export DB_PORT="数据库端口"
export DB_NAME="数据库名"
export DB_USER="数据库用户名"
export SCENARIO_A="场景A标识_首次执行脚本路径"
export SCENARIO_B="场景B标识_首次执行脚本路径"
export SCENARIO_C="场景C标识_首次执行脚本路径"
export SCENARIO_D="场景D标识_首次执行脚本路径"
# ============================================================

# 场景A：全新部署（首次执行全部脚本）
echo "场景A: 执行全新部署脚本集合"
# 【场景A】在此处追加执行命令

# 场景B：增量升级（仅执行增量脚本）
echo "场景B: 执行增量升级脚本集合"
# 【场景B】在此处追加执行命令

# 场景C：回滚操作（按逆序执行回滚）
echo "场景C: 执行回滚脚本集合"
# 【场景C】在此处追加执行命令

# 场景D：校验脚本执行结果
echo "场景D: 执行结果校验"
# 【场景D】在此处追加校验命令
```

#### 4.1.3 Windows PowerShell 四场景模板

```powershell
# ============================================================
#  变量准备区（请在执行前替换以下变量值）
# ============================================================
$DB_HOST = "数据库主机地址"
$DB_PORT = "数据库端口"
$DB_NAME = "数据库名"
$DB_USER = "数据库用户名"
$SCENARIO_A = "场景A标识_首次执行脚本路径"
$SCENARIO_B = "场景B标识_首次执行脚本路径"
$SCENARIO_C = "场景C标识_首次执行脚本路径"
$SCENARIO_D = "场景D标识_首次执行脚本路径"
# ============================================================

# 场景A：全新部署（首次执行全部脚本）
Write-Host "场景A: 执行全新部署脚本集合"
# 【场景A】在此处追加执行命令

# 场景B：增量升级（仅执行增量脚本）
Write-Host "场景B: 执行增量升级脚本集合"
# 【场景B】在此处追加执行命令

# 场景C：回滚操作（按逆序执行回滚）
Write-Host "场景C: 执行回滚脚本集合"
# 【场景C】在此处追加执行命令

# 场景D：校验脚本执行结果
Write-Host "场景D: 执行结果校验"
# 【场景D】在此处追加校验命令
```

### 4.2 历史数据修复 / 回填 / 迁移

- 是否涉及：是 / 否
- 变更说明：
- 对应脚本：
  - `02-db-002-`
- 执行顺序：
- 执行后校验：

### 4.3 菜单 / 权限 / 角色权限 / 字典 / 参数 / 流程配置

- 是否涉及：是 / 否
- 变更说明：
- 对应脚本：
  - `03-config-001-`
- 执行顺序：
- 执行后校验：

## 5. 允许人工操作的事项

### 5.1 修改已有配置文件

- 是否涉及：是 / 否
- 操作文件：
- 操作内容：
- 操作顺序：
- 目标结果：
- 校验方式：

### 5.2 生产环境配置调整

- 是否涉及：是 / 否
- 操作入口：
- 操作内容：
- 操作顺序：
- 目标结果：
- 校验方式：

### 5.3 其他确实无法脚本化的事项

- 是否涉及：是 / 否
- 操作入口：
- 操作内容：
- 操作顺序：
- 目标结果：
- 校验方式：

## 6. 发布执行步骤

1. 核对 `release/version.json` 中的版本号。
2. 执行发布前检查。
3. 按顺序执行 `02-db-xxx` 数据库脚本。
4. 按顺序执行 `03-config-xxx` 配置脚本。
5. 部署应用程序。
6. 执行人工操作事项。
7. 完成发布后验证。
8. 记录发布结果。

## 7. 回滚方案

- 回滚触发条件：
- 数据回滚方式：
- 配置回滚方式：
- 程序回滚方式：
- 回滚注意事项：

每条✅需在发布前补齐对应04、05、06锚点

## 8. 允许人工操作的事项（补充说明）

每条✅需在发布前补齐对应04、05、06锚点

## 9. 发布通知与沟通计划

- 通知对象：
- 通知时间：
- 通知内容：
- 应急预案联系人：

## 10. 发布后观察期安排

- 观察期时长：
- 观察指标：
- 异常处理流程：
- 观察期负责人：

每条✅需在发布前补齐对应04、05、06锚点

## 11. 发布角色×真源文件索引表

| 发布角色 | 真源文件编号 | 文件名称 | 职责说明 |
|---|---|---|---|
|  | 01 | 01-更新手册.md |  |
|  | 02 | 02-db-xxx.sql |  |
|  | 03 | 03-config-xxx.sql |  |
|  | 04 | 04-发布检查清单.md |  |
|  | 05 | 05-发布后验证记录.md |  |
|  | 05-1 | 05-1-功能验收用例(非技术版).md |  |
|  | 06 | 06-版本更新日志.md |  |
|  | version.json | release/version.json |  |

## 12. 附件清单

| 附件编号 | 附件名称 | 对应真源文件 | 说明 |
|---|---|---|---|
| ATT-01 | 更新手册 | 01-更新手册.md |  |
| ATT-02 | 数据库脚本集合 | 02-db-xxx |  |
| ATT-03 | 配置脚本集合 | 03-config-xxx |  |
| ATT-04 | 发布检查清单 | 04-发布检查清单.md |  |
| ATT-04-1 | 功能验收用例（非技术版/UAT） | 05-1-功能验收用例(非技术版).md | UAT验收签字量化依据 |
| ATT-05 | 发布后验证记录 | 05-发布后验证记录.md |  |
| ATT-06 | 版本更新日志 | 06-版本更新日志.md |  |

## 13. 签字区

### 05-1 UAT 签字量化栏

| 量化项 | 数值 | 校验结果 | 备注 |
|---|---|---|---|
| 05-1 P0 用例通过率 |  | ☐ 通过 ☐ 不通过 |  |
| 05-1 P1 用例通过率 |  | ☐ 通过 ☐ 不通过 |  |
| 05-1 验收结论 |  | ☐ 全部通过 ☐ 部分通过 ☐ 不通过 |  |

| 角色 | 姓名 | 签字 | 日期 |
|---|---|---|---|
| 版本负责人 |  |  |  |
| 开发负责人 |  |  |  |
| 测试负责人 |  |  |  |
| 运维负责人 |  |  |  |
| 产品负责人 |  |  |  |
