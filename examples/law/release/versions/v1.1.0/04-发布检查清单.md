# v1.1.0 发布检查清单（release-handbook-manager 标准模板，发布前逐项打勾，禁止跳项）

> **版本**：v1.1.0 / 负责人：待填 / 计划发布日期：待填
> **检查清单结论**：☐ 通过 ☐ 不通过（原因：）
> **执行铁律**：任何标注「一票否决红线」的项不通过时，必须立即停止发布流程打回整改，不得进入后续发布步骤。

---

## 1. 版本真源检查（必过，100% 打勾才能继续）

| 编号 | 检查内容 | 预期结果 | 实际结果 | 通过 ☐ |
| --- | --- | --- | --- | --- |
| 1.1 | [release/version.json](file:///d:/codeConfig/law/release/version.json) 中的 `version` 字段 | `version = v1.1.0`（**必须人工维护，AI 不得自动递增或修改**） |  | ☐ |
| 1.2 | [release/version.json](file:///d:/codeConfig/law/release/version.json) 中的 `previousVersion` 字段 | `previousVersion = v1.0.0` |  | ☐ |
| 1.3 | [release/version.json](file:///d:/codeConfig/law/release/version.json) 中的 `status` 字段 | 开发阶段 = `developing`；正式发布前人工改成 `released` + 填 `releaseDate` + 填 `owner` |  | ☐ |
| 1.4 | `release/versions/` 目录下的版本目录结构（LS 核查）| 仅保留 `v1.1.0/`（`v1.0.0` 作为历史归档若存在可保留）；**不得再出现 v1.2.0 / v1.3.0 / v1.4.0+ 任何目录**（一票否决红线，违反打回） |  | ☐ |
| 1.5 | 当前 `v1.1.0/` 目录结构是否符合 rhm Skill 单层平铺标准 | 目录下只有 `01-更新手册.md / 02-db-*.sql / 03-config-*.sql（如有）/ 04-发布检查清单.md / 05-发布后验证记录.md / 05-1-发布验收功能清单.md / 06-版本更新日志.md` 7 类标准文件；<br>**不得存在「原XXX-参考.md」类历史残留文件**；<br>**不得存在 2026XXXX-Windows-Terminal 临时命令类文件**（违反打回清理） |  | ☐ |
| 1.6 | 版本说明口径是否与 01-更新手册一致 | 01 §1 基本信息 5 项（版本号/上一版/状态/负责人/计划时间）与本 1.1~1.5 完全一致 |  | ☐ |

---

## 2. 发布材料完整性检查（rhm 标准 01 / 02 / 03 / 04 / 05 / 06 全齐）

| 编号 | 标准文件名（`release/versions/v1.1.0/` 下） | 路径是否存在（LS 核查）| 人工检查内容（逐项打勾） | 通过 ☐ |
| --- | --- | --- | --- | --- |
| 2.1 | `01-更新手册.md` | ☐ 是 ☐ 否 | 01 §3 强制登记范围 11 项（结构变更/字段/索引/存储过程/历史修复/菜单权限/字典流程/配置/生产配置/人工操作/回滚）全部有记录，无「待补」「现场处理」等模糊语句 |  | ☐ |
| 2.2 | `02-db-001` ~ `02-db-013`（共 13 份数据库脚本） | ☐ 是 ☐ 否 | 13 份全存在；编号 1~13 连续无跳号；每份文件名前缀严格是 `02-db-`（批次/段别/业务说明）` |  | ☐ |
| 2.3 | `03-config-001` ~ `03-config-002`（菜单/权限/角色权限配置脚本） | ☐ 是 ☐ 否 | 两份脚本均存在；`03-config-001` 覆盖菜单/按钮节点，`03-config-002` 覆盖默认角色授权；01 §4.2 与 06 更新日志不得再出现“本版未输出 03-config”旧口径 |  | ☐ |
| 2.4 | `04-发布检查清单.md`（本文件） | ☐ 是 ☐ 否 | 已填本文件打勾，无漏项 |  | ☐ |
| 2.5 | `05-发布后验证记录.md` | ☐ 是 ☐ 否 | 05 中 P4-A 18 项 + P4-B 10 项 + P4-C 6 项 + S01 18 项 = 49 项验证用例齐全；+ **75 项 dry-run（§7.1~§7.5）** 齐全；+ 失败待修复列表齐全 |  | ☐ |
| 2.5-1 | `05-1-发布验收功能清单.md` | ☐ 是 ☐ 否 | §2.5-1-1 功能覆盖率：本版 §1 全部功能点逐条登记（含批次1~4），无遗漏；<br>§2.5-1-2 缺陷回归：01 §11 已知问题列表中标记已修复项，逐项对应 05 中回归通过记录；<br>§2.5-1-3 技术术语0命中：05-1 面向业务验收人员文案中，不得出现 bizType/denorm/cache TTL/双写/40310 等技术黑话；<br>§2.5-1-4 8列固定结构一致性：整份 05-1 严格按「编号/功能点/验收场景/前置条件/操作步骤/预期结果/实际结果/签字」8 列固定表头，缺列即判定不通过 |  | ☐ |
| 2.6 | `06-版本更新日志.md` | ☐ 是 ☐ 否 | 06 与 01 §10 变更总表一一对应，新增/优化/修复/兼容说明 5 段齐全；版本信息段填 v1.1.0 / previous v1.0.0 |  | ☐ |
| 2.7 | AI 版本号禁令自检（rhm Skill §2.4 一票否决红线） | - | AI 未在任何无人工明确指令下自动修改 version.json version 字段；最近 10 次 Git 提交记录中 version 文件未被 AI 自动 bump；若违反直接打回 |  | ☐ |

---

## 3. 数据库脚本幂等与逻辑核查（每条 SQL 2 次执行，第二次 0 报错）

| 编号 | 核查项 | 执行方法（复制即可） | 预期结果 | 实际结果 | 通过 ☐ |
| --- | --- | --- | --- | --- | --- |
| 3.1 | 批次 1 脚本 `02-db-001~003` 同一脚本连续执行 2 次 | 开发库顺序跑 001→003，再重跑 001→003 第二次 | 第二次执行 0 DDL 报错、0 UPDATE 异常、0 UNIQUE 冲突 |  | ☐ |
| 3.2 | 批次 3 P4-A 脚本 `02-db-004~011` 同一脚本连续执行 2 次 | 开发库 004→011 两遍 | 第二次 0 报错，特别关注 010（索引）、011（全量镜像 + 冲突自动清洗）的幂等 |  | ☐ |
| 3.3 | 批次 4 S01 脚本 `02-db-012~013` 同一脚本连续执行 2 次 | 开发库 012→013 两遍（013 尤其要测 IF EXISTS） | 第二次 0 报错；013 DROP IF EXISTS 第二次执行 0 错误 |  | ☐ |
| 3.4 | O01-补 02 策略 A ID 冲突自动清洗校验（011 STEP 0 段） | 手工造冲突：INSERT base_sys_department id=99999；INSERT case_team id=99999 → 执行 011 | 011 自动完成冲突清洗并输出 `remainingConflictCount=0`；随后成功进入镜像步骤 |  | ☐ |
| 3.5 | O01-修 01 isPrimary 原子化修复段（010 组合索引的修复 SQL） | 构造 1 用户 2 条 isPrimary=1 的脏数据 → 执行 010 的修复段 | 保留 id 最小那条 isPrimary=1，其余自动置 0 |  | ☐ |
| 3.6 | S01 方案② 6 类列/表 DROP 脚本幂等（013） | 手工造 CREATE TABLE case_relation(id INT); ALTER TABLE case_stop_end_apply ADD newCaseId INT; → 跑 013 → 再重跑 013 | 第一次 DROP 成功；第二次 0 报错；DESC / SHOW TABLES 检查残留列/表 = 0 |  | ☐ |
| 3.7 | SQL 编号段与 01 §4.1.1 的 4 场景映射一致性 | 对照 01 §4.1.1 场景表 A/B/C/D 分别核对 001~013 编号段 | 场景 A 13 条=001~013 / 场景 B 10 条=004~013 / 场景 C 8 条=004~011 / 场景 D 2 条=012~013 全部匹配 |  | ☐ |
| 3.8 | 不可逆脚本 02-db-013 人工二次确认签字（rhm Skill 要求） | 01 §12 签字区对应「门禁签字」| 业务/架构/运维三方已签字同意执行 013；若未签字禁止执行 013 |  | ☐ |

---

## 4. 代码与框架核查（O01 红线索引 + S01 双层兜底全部落地，非文档声明）

| 编号 | 核查项（后端/前端） | 执行方法 | 预期结果（grep/代码 Read/接口验证） | 实际结果 | 通过 ☐ |
| --- | --- | --- | --- | --- | --- |
| 4.1 | 组织结构 4 个新 Service + 4 个 Controller 基础 CRUD | 代码 Read 后端路由：`/base/sys/orgSubject`、`/base/sys/orgUnit`、`/base/sys/orgMember`、`/base/sys/orgRoleScope` | 4 实体 × 6 接口（add/list/update/delete/info/page）= 24 接口全部存在；每个接口 summary 为简体中文（非空） |  | ☐ |
| 4.2 | **tenantId 强制覆盖**<br>（一期铁规，一票否决） | PowerShell grep：后端 4 新<br>Service 的 add/update 方法首行 | `Get-ChildItem backend/.../org -Include *.ts`<br>\| Select-String `'param\.tenantId\s*=\s*1'`<br>→ **16 处命中**（4 add + 4 update × 2 实体）；<br>无条件覆盖；前端传 9999，DB 实际存 1 |  | ☐ |
| 4.3 | O01-修 01 isPrimary 原子化 SQL | 单语句 CASE WHEN | `OrgMemberService.atomicSetPrimary` 方法代码 | UPDATE 语句 CASE WHEN 单语句；**禁止 SELECT+UPDATE 两步**（并发安全） |  | ☐ |
| 4.4 | O01-修 03 40016 / 40017 代码命中 | grep 后端错误码 | 构造 9 层深 parentId 保存 org_unit → 返回 40016；构造 case_team parentId=null → 返回 40017 | 错误码命中 40016/40017 分别对应「组织层级超 8 层 / 办案组必挂部门下 |  | ☐ |
| 4.5 | O01-修 04 数据权限唯一真源写入 | grep 后端新缓存 `admin:orgScope:{roleId}` 生成位置 | P4-B 期仅写 admin:orgScope:*；旧缓存 admin:department:* 只读不写（P4-A 期保留兼容读） |  | ☐ |
| 4.6 | 双写切面失败口径（PHASE=A 不回滚 + PHASE=B 500 回滚）| 代码 Read `OrgSyncDualWriteService routeWriteFail 函数 14 处 catch | 14 处 catch 全部接入 routeWriteFail；PHASE=A → logWarn 不抛；PHASE=B → throw 抛 CoolCommException 500 |  | ☐ |
| 4.7 | S01 9 行拦截矩阵（assertStopEndMatrix 方法） | 代码 Read `CaseStopEndService.assertStopEndMatrix` | 行 2=40093 / 行 4=40094 / 行 6/7/8=40310 / 行 1/3/5 放行；行 9 审批链自身豁免 | 9 行矩阵与 01 §10 / 05 S01-01~S01-18 完全对应 |  | ☐ |
| 4.8 | S01 终止最终关 4 级必须「司法局管理员 JusticeAdmin（40099）| 代码 Read approveApply 终止 step=4 | approverRoles 不包含 JusticeAdmin → 抛 40099；满足则通过 | 错误码 40099 命中 1 处，唯一使用场景 |  | ☐ |
| 4.9 | S01 fromStopEndService 乐观锁 409（防并发篡改）| 代码 Read approveApply 开头快照 vs 审批通过前 fresh 状态 | 快照 != 当前 → 抛 409；错误消息包含「并发冲突：发起时快照 stopEnd=X，当前 stopEnd=Y」 |  | ☐ |
| 4.10 | **方案② 6 类关键字全文 grep 门禁**<br>（S01 §9.4 硬要求，一票否决） | 执行 §4.1 PowerShell 命令<br>（复制粘贴到终端即可） | 后端非注释命中 **0 条**；<br>前端非注释命中 **0 条**；<br>注释命中仅允许 0 条或纯解释性说明 |  | ☐ |
| 4.11 | PHASE 开关初始锁定 PHASE='A'（P4-A 双写对账期） | `grep -n "PHASE:" backend/src/modules/base/service/org/org_gray_switch.ts` | 结果仅 1 条命中 `readonly PHASE: 'A' \| 'B' \| 'C' = 'A'`；**未签字情况下 B/C 禁止出现** |  | ☐ |
| 4.12 | 审计日志 9 字段（继承批次 1 口径 20260714-07 协作规则）| 关键接口 add/approve/reject 走原有审计中间件 | 9 字段齐全：结果状态 / 状态码 / 错误摘要 / 业务对象类型 / 业务对象 ID / 请求标识 + userId / action / ip | 命中 0 遗漏 |  | ☐ |
| 4.13 | client 端注销规则（20260714-06 协作规则，本版沿用）| grep client logout 相关代码 | logout 接口先调服务端 `/admin/sys/logout 注销 → 再清 localStorage token/user/role；顺序不能反 | ☐（本批次未动 client，沿用前版已验证通过 |  | ☐ |
| 4.14 | 本轮新增 12 接口 summary 全部为简体中文非空（rhm §3.2 登记）| 读 backend 12 个新增 Controller 路由 @CoolTag 或 @CoolRoute summary | 12 接口=autoAssignCandidates(1) + stopEndManage 7接口 + dict import&whitelist(2) + org4实体 page(4 预留)；每条 summary 为简体中文非空字符串；禁止英文或空 summary | ☐（EPS 生成后菜单权限才能回显，本项先静态代码核查 summary 存在性）|  | ☐ |
| 4.15 | **110 分 Top5 分案算法**<br>6 字段明细 grep ≥ 1 处命中<br>（P3-14 算法落地） | 后端 PowerShell grep：<br>`sortKey\|orgScore\|minCaseScore`<br>`\|roleScore\|score\|calcOrgScore` | 6 字段在以下 3 处合计 ≥ 6 命中：<br>① info.ts ② denorm_backfill.ts ③ team.ts；<br>证明算法非空壳实现，有实际计算逻辑 | ☐（验证 assign 事务中<br>denorm 3 字段同步写入） |  | ☐ |
| 4.16 | 9 行矩阵 5 类新错误码 grep 各 ≥ 1 处命中（S01 §10 40311~40315）| 后端 grep `40311\|40312\|40313\|40314\|40315` 错误码 | 5 码各 ≥ 1 处实际调用（非注释）：40311=主流程提交拦截 / 40312=主表编辑拦截 / 40313=终止资料上传拦截 / 40314=讨论区发布拦截 / 40315=律所端自查报告拦截；全部命中才算通过 | ☐（与 assertStopEndFlowMatrix 行 1~5 一一对应）|  | ☐ |
| 4.17 | **审计 6 个 stop_end_* 枚举**<br>count = 6（S1-8 审计拆分） | 后端 grep bizType 枚举声明：<br>`STOP_END_APPROVE\|_STATUS_CHANGE`<br>`\|_TODO_CLOSE\|_MSG_SEND`<br>`\|_FLOW_END\|_EMPTY_APPROVER` | ① log.ts 中 6 枚举全部声明 count=6；<br>② info.ts 终审分支 5 条 INSERT +<br>空审批人 1 条 INSERT = 6 条 bizType 全命中 | ☐（验证审批通过不再<br>合并为 1 条通用审计） |  | ☐ |
| 4.18 | 三个新增错误码 40901/40902/50001 grep ≥ 1 处命中（处分结构化冲突+空审批人兜底）| 后端 grep `40901\|40902\|50001` | 40901=无摘要却有处分结果冲突 / 40902=有摘要却无结果冲突 / 50001=空审批人500兜底；每码 ≥ 1 处 throw CoolCommException 实际调用（非注释） | ☐（与 B1-5 处分结构化 + S1-5 空审批人漏洞修复完全对应）|  | ☐ |
| 4.19 | 前端独立中止终止管理页路由存在性 + 2 组件注册核查（S1-4 独立管理页）| 前端 grep `/case/stop-end-manage\|stop-end-manage/index.vue\|apply-status-tag\|process-step` 四段字符串 | frontend/src/modules/case/config.ts 存在 path='/case/stop-end-manage' 路由；views/stop-end-manage/ 下 index.vue + detail.vue 两文件存在；components/stop-end/ 下 apply-status-tag.vue + process-step.vue 两个公共组件存在 | ☐（与 case:stopEndManage:page 权限标识配套；路由 keepAlive=true） |  | ☐ |
| 4.20 | 兜底 3 白名单权限标识在 EPS 接口权限可搜索回显（rhm §3.2 登记）| EPS 生成后，登录 admin，菜单 /sys/menu 权限下拉框搜索 3 串：`case:stopEnd:emergencyAssignApprover\|case:stopEndManage:reviewInfo\|case:team:autoAssignCandidates` | 3 权限标识在下拉框可搜到；/sys/role 「数据豁免权」按钮下 allData 权限可回显（rhm §12 业务规则 allData 口径）；12 新增接口 EPS 生成后 perms 字符串与 01 §3.2 登记完全一致 | ☐（本项必须在 EPS 重新生成后人工执行，当前仅静态代码核查 Controller 路由注解中对应字符串存在） |  | ☐ |
| 4.21 | denorm 回填服务 2 入口方法存在性 + 4 触发点 grep（P3-9 denorm 防统计漂移）| 后端 grep 方法名 `backfillAllDenormSnapshot\|backfillDenormSnapshotByCaseId`；再 grep 4 触发点 assign()/updateHandler()/updateGroupOrgId()/info() 内回填段 | backend/src/modules/case/service/denorm_backfill.ts 存在两方法声明与实现；4 触发点在 backend/src/modules/case/service/info.ts 中 grep 到 4 处 denorm 写入段（非注释）；info() 读时回填段有 try/catch 包裹不阻塞主流程 | ☐（验证回填只填 NULL 不覆盖，幂等设计 fallback 旧 CaseTeamEntity.departmentId） |  | ☐ |
| 4.22 | **05-1 发布验收功能清单专项核查**<br>（4 子项必须全部通过） | 对照 `05-1-发布验收功能清单.md`<br>逐项核查以下 4 子项： | §4.22-1 **功能覆盖率**：05-1 登记功能点与 01 §1、06 §1 三方一一对应，无遗漏；<br>§4.22-2 **缺陷回归 100%**：01 §11 标记已修复缺陷在 05 中全部有回归通过记录，0 项悬而未决；<br>§4.22-3 **技术术语 0 命中**：05-1 全文面向业务方验收，不得出现 bizType/错误码/缓存 TTL/denorm/双写 等技术黑话；<br>§4.22-4 **8 列固定结构一致性**：05-1 整份文档严格使用「编号/功能点/验收场景/前置条件/操作步骤/预期结果/实际结果/签字」固定 8 列表头，列数/列序不得增删调换 | ☐（§4.22 四项全通过方可<br>进入 §7 最终结论签字） |  | ☐ |

### 4.1 方案② 6 类关键字 grep 命令（发布前执行，复制粘贴，PowerShell）

```powershell
# ---------- 后端代码 non-comment hit count = 0 ----------
Get-ChildItem -Path 'backend/src' -Recurse -Include *.ts,*.sql |
  Select-String -Pattern '\b(newCaseId|sourceCaseId|case_relation|restoreRemark|relationType|resume_copy|restoredCaseId|parentCaseId)\b' |
  Where-Object { $_.Line -notmatch '^\s*//' -and $_.Line -notmatch '^\s*\*' } |
  Select-Object -First 50
# 预期：0 条命中（或全部是解释性注释命中，如 stop_end_apply.ts 的「删除标记」说明段，非实际代码使用）

# ---------- 前端代码 non-comment hit count = 0 ----------
Get-ChildItem -Path 'frontend/src' -Recurse -Include *.ts,*.vue,*.tsx,*.sql |
  Select-String -Pattern '\b(newCaseId|sourceCaseId|case_relation|restoreRemark|relationType|resume_copy|restoredCaseId|parentCaseId)\b' |
  Where-Object { $_.Line -notmatch '^\s*//' -and $_.Line -notmatch '^\s*\*' } |
  Select-Object -First 50
# 预期：0 条命中
```

---

## 5. 部署前环境核查（MySQL / Midway / Vite 生产配置）

| 编号 | 核查项（生产配置，违反将直接导致发布事故） | 预期结果 | 实际结果 | 通过 ☐ |
| --- | --- | --- | --- | --- |
| 5.1 | MySQL 版本 ≥ 8.x（JSON_TABLE 语法依赖，011 存量镜像脚本用到） | SELECT VERSION() ≥ 8.0.x；否则 011 脚本报错 SQL 语法错误 |  | ☐ |
| 5.2 | Midway ≥ 3.0 + @midwayjs/cache-manager TTL 正常 | admin:orgScope:{roleId} 新缓存 TTL = 6h；Redis/MemoryCache 正常写读 |  | ☐ |
| 5.3 | TypeORM synchronize **禁用**（生产环境一票否决红线） | production.json 或 config.prod.ts 中 `synchronize: false`；违反 = 禁止上线，防止自动 DDL 漂移 |  | ☐ |
| 5.4 | 应用重启预热 5min 观察错误日志 | 启动后 5min 0 ERROR；双写 WARN 仅允许 1 级不允许堆 ERROR 栈；无 DB 连接池耗尽 |  | ☐ |
| 5.5 | 部署顺序与 01 §6.2 一致 | 先后端 → 再前端；跑 §4 SQL 成功 → 部署前先备库备份最新一份冷备 → 再启应用 |  | ☐ |
| 5.6 | 发布窗口与 PHASE 一致性 | 未切 PHASE 时部署后 grep 仍是 PHASE='A'；切 B/C 必须先在 01 §12 签字后方可部署对应代码 |  | ☐ |

---

## 6. 发布前人工确认与签字

### 6.1 人工操作 5 项执行时间窗口确认

| 编号 | 介入点（对应 01 §5.3） | 是否具备执行条件（是 / 否 / 不涉及） | 备注（窗口未到则备注「待 XX 日执行」） | 责任人签字 |
| --- | --- | --- | --- | --- |
| 6.1.1 | 介入点 ① 开发库跑 SQL 004→013（10 条，场景 B） |  |  |  |
| 6.1.2 | 介入点 ② 5 角色审批链 12 张截图 |  | 5 账号已准备：组长/主任/副会长/司法局管理员/普通申请人 |  |
| 6.1.3 | 介入点 ③ P4-A 双写对账（第 7/14 天 2 次 |  | 需等待真实日历时间，禁止手工改 DB 时间模拟 |  |
| 6.1.4 | 介入点 ④ 40310 并发 10 次压测（S01 上线前 48h） |  | 真实部署地址与 token 已具备 |  |
| 6.1.5 | 介入点 ⑤ PHASE 切 B / 切 C / 物理删表 3 次签字 + 1 行代码改动 3 次 |  | 每次签字前置 |  |

### 6.2 签字区（rhm 标准五角色签字，缺一不可）

| 角色 | 签字 | 日期 | 备注 |
| --- | --- | --- | --- |
| 开发负责人（02-db 13 份全本库逐行验证） |  |  | 02-db-013 不可逆脚本重点核查 |
| DBA（02-db 加列锁表影响评估 + 备份执行计划）|  |  | 生产发布窗口建议 / 锁表风险评估 |
| 测试负责人（发布前冒烟 49 项 + 75 dry-run ≥ 95% 通过） |  |  | 未通过项清单附 05 §失败列表 |
| 产品负责人（组织结构 O01-修 01~07 9 大红线业务签字）|  |  | 见 01 §2.1 组织结构真源业务确认通过 |
| 产品负责人（S01 40310 终止不可逆最终口径签字）|  |  | 见 01 §2.1 S01 口径确认；全员培训 40310 培训记录附件 |
| 运维负责人（部署顺序 / 回滚方案 / PHASE 切换部署包准备） |  |  | 3 套包齐备：A 包 / B 包 / C 包 / 紧急 B→A 回滚包 |

---

## 7. 最终结论（本检查清单全部完成后，由发布负责人填写）

- 本清单总项数：`1.1~1.6=6 / 2.1~2.7 + §2.5-1=8 / 3.1~3.8=8 / 4.1~4.22=22 / 5.1~5.6=6 / 6.1~6.2 = 合计 56 + 签字 6 = **62 子项**（其中 **§4.22 = 05-1 专项核查 4 子项必须全部通过**，一票否决；4.14~4.22=9 项为本轮新增能力的发布前门禁；`02-db-011` 已内置 O01-补02 冲突自动清洗核查）
- 一票否决前置核验：☐ §4.22（05-1 专项核查 4 子项）**全部通过**（此项未打勾禁止进入最终结论签字）
- 通过项数：____ / 不通过项数：____ / 不涉及项数：____ / 通过率：____ %
- 最终结论：☐ 全部通过（含 §4.22 必须通过），允许进入 01 §6.2 标准发布执行步骤；☐ 存在红线未过（含 §4.22 任一子项不通过），打回整改（整改后重新跑本检查清单）
